apiVersion: v1
kind: Service
metadata:
  name: movie-app-service
  namespace: default
spec:
  selector:
    app: movie-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: LoadBalancer

